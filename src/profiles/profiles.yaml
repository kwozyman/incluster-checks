profiles:
  # --- Layer 1: Core ---
  general:
    description: "Global common rules"

  # --- Layer 2: Domains ---
  telco-base:
    include: [general]
  ai-base:
    include: [general]
  network-foundations:
    description: "Network foundations validation profile"
    include: [general]

  # --- Layer 3: Platforms & Hardware ---
  gpu:
    include: [ai-base] # GPU implies AI-base rules
  spectrum-x:
    description: "NVIDIA Spectrum-X networking platform for AI factories"
    include: [general]
  rh-nokia:
    include: [telco-base]

  # --- Layer 4: Specific Implementations ---
  nokia-qa:
    include: [rh-nokia]
  llm-d-xks:
    description: "llm-d validation profile for XKS deployments"
    include: []

